In order to run scheduled functions locally:

- firebase functions:shell

Once inside the Firebase functions shell, simply enter to start looping (30 seconds here):

- setTimeout(getCoinPrice, 30000)
