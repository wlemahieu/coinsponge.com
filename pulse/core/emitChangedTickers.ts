/**
 * emitChangedTickers.ts
 *
 * Emit all updated tickers
 */
import { WebSocketServer } from 'ws';

export default async (wss: WebSocketServer, tickers: Array<any>) => {
  return Promise.resolve();
};
